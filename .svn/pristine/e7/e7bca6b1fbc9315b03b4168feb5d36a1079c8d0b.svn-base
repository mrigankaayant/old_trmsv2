<ui:composition xmlns="http://www.w3.org/1999/xhtml" xmlns:ui="http://java.sun.com/jsf/facelets"
	xmlns:h="http://java.sun.com/jsf/html" xmlns:f="http://java.sun.com/jsf/core" xmlns:p="http://primefaces.org/ui">

	<p:growl id="growl" showDetail="true" sticky="false" autoUpdate="true" />

	<h:form id="form">
		<p:panel>

			<f:facet name="header" style="text-align:center">
				<p:outputLabel>Running All Batch</p:outputLabel>
			</f:facet>


			<p:dataTable var="p" value="#{trainerBean.programmeSchedules}" paginator="true" rows="20" rowKey="{#{p.id}}"
				selection="#{trainerBean.programmeSchedule}" resizableColumns="true" resizeMode="true"
				paginatorTemplate="{RowsPerPageDropdown} {FirstPageLink} {PreviousPageLink} {CurrentPageReport} {NextPageLink} {LastPageLink}"
				sortBy="#{p.id}" sortOrder="descending" rowsPerPageTemplate="10,25,50,100" filterEvent="change"
				id="programeschedule-table" lazy="true" widgetVar="programescheduleTable" tableStyle="table-layout: auto">


				<p:column selectionMode="single" style="width:16px;text-align:center" />

				<p:column headerText="Id" style="text-align:center">
					<h:outputText value="#{p.id}" />
				</p:column>

				<p:column headerText="Course Name" style="text-align:center">
					<h:outputText value="#{p.candidateCourse.course}" />
				</p:column>

				<p:column headerText="Trainer Name" style="text-align:center">
					<h:outputText value="#{p.trainer.trainerName}" />
				</p:column>

				<p:column headerText="Start Date" style="text-align:center">
					<h:outputText value="#{p.startDate}" />
				</p:column>
				
				<p:column headerText="End Date" style="text-align:center">
					<h:outputText value="#{p.endDate}" />
				</p:column>
				
				<p:column headerText="Start Time" style="text-align:center">
					<h:outputText value="#{p.startTime}" />
				</p:column>
				
				<p:column headerText="End Time" style="text-align:center">
					<h:outputText value="#{p.endTime}" />
				</p:column>
				
				<p:column headerText="Day Of Week" style="text-align:center">
					<h:outputText value="#{p.dayOfWeek}" />
				</p:column>

			</p:dataTable>
			
			<f:facet name="footer" style="text-align:left">
				<p:commandButton value="Next" action="#{hrBean.candidateListForAssign()}" onclick="PF('statusDialog').show()" oncomplete="PF('statusDialog').hide()" update=":mainBody" />
			</f:facet>
		</p:panel>

	</h:form>
</ui:composition>